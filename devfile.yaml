schemaVersion: 2.2.0
metadata:
  name: test-web-internal
components:
  - name: web
    container:
      image: quay.io/devfile/universal-developer-image:ubi9-latest
      memoryLimit: 512Mi
      endpoints:
        - name: http
          targetPort: 8080
          exposure: internal
          protocol: http

